{{!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Signup | Buy Crops</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f7f8fa; /* light background consistent with project */
    }
    .card {
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    .btn-primary {
      background-color: #28a745; /* green consistent with farm theme */
      border-color: #28a745;
    }
    .btn-primary:hover {
      background-color: #218838;
      border-color: #1e7e34;
    }
  </style>
</head>
<body>
  <div class="container d-flex justify-content-center align-items-center" style="height: 100vh;">
    <div class="card p-4" style="width: 400px;">
      <h3 class="text-center mb-3">Signup</h3>
      <form action="/signup" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label">Name</label>
          <input type="text" name="name" class="form-control" id="name" placeholder="Enter your name" required>
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input type="email" name="email" class="form-control" id="email" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="password" name="password" class="form-control" id="password" placeholder="Enter password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Signup</button>
      </form>
      <p class="text-center mt-3">
        Already have an account? <a href="/login">Login here</a>
      </p>
    </div>
  </div>
</body>
</html> --}}


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Signup | Buy Crops</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f7f8fa;
      font-family: "Inter", sans-serif;
    }
    .auth-card {
      border-radius: 16px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.08);
      transition: 0.3s ease;
    }
    .auth-card:hover {
      box-shadow: 0 12px 28px rgba(0,0,0,0.12);
    }
    .btn-success {
      background-color: #28a745;
      border: none;
      transition: 0.2s ease;
      font-weight: 500;
    }
    .btn-success:hover {
      background-color: #218838;
      transform: translateY(-1px);
    }
    .alert-custom {
      background-color: #fef1f0;
      border-left: 4px solid #d93025;
      color: #d93025;
      border-radius: 6px;
      padding: 10px 15px;
      font-size: 0.9rem;
    }
    .password-strength {
      font-size: 0.85rem;
      color: #888;
    }
  </style>
</head>
<body>
  <div class="container d-flex justify-content-center align-items-center" style="height:100vh;">
    <div class="auth-card bg-white p-4" style="width:400px;">
      <h3 class="text-center mb-4 fw-semibold">Create Your Account 🌱</h3>

      {{#if error}}
      <div class="alert-custom mb-3">
        {{error}}
      </div>
      {{/if}}

      <form action="/signup" method="POST" novalidate>
        <div class="mb-3">
          <label for="name" class="form-label">Full Name</label>
          <input type="text" name="name" id="name" class="form-control" placeholder="Enter your name" required>
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email address</label>
          <input type="email" name="email" id="email" class="form-control" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="password" name="password" id="password" class="form-control" placeholder="Create a strong password" required>
          <div id="passwordHelp" class="password-strength">At least 6 characters</div>
        </div>
        <button type="submit" class="btn btn-success w-100 py-2">Create Account</button>
      </form>

      <p class="text-center mt-3 text-muted">
        Already have an account? <a href="/login" class="text-success text-decoration-none fw-medium">Login</a>
      </p>
    </div>
  </div>
</body>
</html>
