<nav class="navbar navbar-expand-lg py-3.9 bg-success">
  <div class="container d-flex align-items-center justify-content-between">
    <!-- Brand section -->
    <a class="navbar-brand ms-3 d-flex align-items-center position-relative" href="#" onclick="openLogoModal()">
      <img src="{{data.brandIconUrl}}" alt="Logo" class="logo-big">
      <span class="text-white fw-semibold ms-5">{{data.brandName}}</span>
    </a>

    <!-- Navbar toggler (for mobile) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar links -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        {{#each data.links}}
        <li class="nav-item">
          <a class="nav-link text-white fw-medium px-3" aria-current="page" href="{{this.url}}">
            {{this.label}}
          </a>
        </li>
        {{/each}}
      </ul>
    </div>
  </div>
</nav>

 {{!-- Logo Modal  --}}
<div class="modal fade" id="logoModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content border-0 bg-transparent d-flex justify-content-center align-items-center">
      <img id="logoModalImg" src="{{data.brandIconUrl}}" alt="Logo"
        style="max-width: 90%; max-height: 80vh; border-radius: 10px;">
    </div>
  </div>
</div>
<style>

.navbar {
  background-color: #2e8b57 !important; 
}

.navbar-nav .nav-link:hover {
  color: #d4edda !important; 
  text-decoration: underline;
}

.navbar-toggler {
  border-color: white;
}

.navbar-toggler-icon {
  background-color: white;
}

.logo-big {
  height: 28px;
  transform: scale(1.8);
  border-radius: 8px;
  cursor: pointer;
  transition: transform 0.2s;
}

.logo-big:hover {
  transform: scale(1.2);    
}

.navbar-brand span {
  font-size: 1.6rem;       
  font-weight: 600;        
  line-height: 1;          
  white-space: nowrap;     
}

</style>

<script>
  function openLogoModal() {
    const modalImg = document.getElementById('logoModalImg');
    const logoModal = new bootstrap.Modal(document.getElementById('logoModal'));
    logoModal.show();
  }
</script>